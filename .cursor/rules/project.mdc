---
description: MAX 精英賽 Telegram 機器人專案：架構、技術棧與 AI 學習目標
alwaysApply: true
---

# 專案概述

- **名稱**：max-elite-telegram-bot
- **目標**：從定時推送 MAX 帳戶餘額與市價起步，擴充為「具備 RAG (檢索增強生成) 與 Embedding 能力的 AI 應用學習專案」。
- **核心原則**：全程使用「完全免費」方案進行開發與部署。

# 系統架構與分工

本專案採用微服務雛形架構，各模組職責明確：

1. **排程執行器 (GitHub Actions)**
   - 負責定時觸發推播任務（目前每 2 小時）。
   - 執行根目錄的 `send-balance.js` 腳本。
2. **資料與邏輯中心 (NestJS 後端 - 部署於 Render)**
   - 負責與 MAX API 溝通（取得餘額 `/balance`、市價 `/tickers`）。
   - 負責組裝 Telegram 訊息格式（`/telegram/message`）。
   - 未來作為資料庫讀寫、前端儀表板的資料提供者。
3. **AI 與爬蟲處理層 (Python - 未來擴充)**
   - 負責爬取幣圈新聞。
   - 處理 Embedding（向量化）並存入向量資料庫。
   - 實作 RAG 邏輯，結合 LLM（如 Gemini API）回答 Telegram 提問。
4. **資料庫層 (Supabase / pgvector - 未來擴充)**
   - 儲存歷史餘額、市價。
   - 儲存新聞向量資料供 RAG 檢索使用。

# 約定與習慣

- **金鑰與敏感資訊**：絕對不 commit。本機放 `.env`，雲端放 GitHub Secrets 或 Render Environment Variables。
- **單一資料來源**：`send-balance.js` 不再直接呼叫 MAX API，而是向 NestJS 取得組裝好的資料。
- **錯誤處理**：優先使用 `try-catch` 骨架；讀取環境變數時優先使用 `getOrThrow` 提早暴露錯誤。
- **免費額度控管**：Actions 排程勿過密，注意 Render 休眠機制的冷啟動時間。
